{
  "name": "zknftmint",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "weijiekoh",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "typescript": "^4.4.4"
  }
}
